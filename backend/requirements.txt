flask==3.0.3
flask-cors==4.0.1
python-dotenv==1.0.1
faster-whisper==1.0.3
# TTS on macOS uses the built-in `say` + `afconvert` (no pip dep needed)
sentence-transformers==3.0.1
pymongo==4.8.0
requests==2.32.3
numpy==1.26.4
